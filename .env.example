# Backend
REDIS_URL=redis://redis:6379/0
UPLOAD_DIR=/app/data/uploads
OUTPUT_DIR=/app/data/outputs

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8000

# WORKER_ID is NOT set here — it is declared per-container in docker-compose.yml
